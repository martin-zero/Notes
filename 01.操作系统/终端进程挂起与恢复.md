---
tags:
  - Linux开发
date: 2026-05-23
---
# 终端进程挂起与恢复

可以使用 `ctrl + z` 暂时将前台进程暂停并挂起到后台

`fg`命令可以快速恢复后台挂起的进程，后边可以带上编号指定恢复的进程
```sh
fg
fg %1
```

`bg`命令可以让当前暂停的任务转移到后台运行
```sh
bg
bg %1
```

使用`jobs`可以查看所有挂起的进程
```sh
jobs
```