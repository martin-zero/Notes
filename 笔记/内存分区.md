---
tags:
  - Subsystem/Memory
---

## 1. 栈区

由编译器管理，存放函数的局部变量、形参、返回地址等。 先进后出。

## 2. 堆区

用户手动开辟的内存，需要手动释放。 先进先出。

## 3. 全局区

分为.bss和.data两部分组成，.bss储存未初始化全局变量和static静态变量、.data储存已初始化全局变量和静态变量。 全局区仅初始化一次，未初始化的变量在程序开始时自动清零。

## 4. 常量区

存放常量（字符串或者const常量）和程序代码。 通常只读。