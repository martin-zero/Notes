## 左值 (lvalue, Left Value)

- **定义**：具有持久标识符（可以取地址、有名字）的表达式。
- **特点**：可以出现在赋值符号的左边或右边，生命周期长，不随表达式结束而销毁。
- **例子**：
    - 变量名：`int a = 10;` 
    - 解引用：`*ptr`
    - 数组下标：`arr[i]`
    - 返回左值引用的函数调用：`std::cout << "hi"`
- **引用方式**：左值引用 (`T&`)。